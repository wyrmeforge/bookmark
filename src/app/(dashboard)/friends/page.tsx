import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Friends | YOOKOSO',
  description: 'Friends page - YOOKOSO',
};

export default function FriendsPage() {
  return <div>fRiend s page</div>;
}
